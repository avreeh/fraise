<!doctype html>
<html lang = "en">
<head>
	<title>fraise - learn conversational French</title>
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<script src="js/jquery-1.11.0.js"></script>
	<script src="js/lesson.js"></script>
	<script src="js/bootstrap.js"></script>
	<link href="css/bootstrap.css" rel="stylesheet">
	<link href="css/bootstrap-theme.css" rel="stylesheet">
	<link href="css/fraise.css" rel="stylesheet">
</head>
<body>
	<nav class="navbar navbar-default navbar-fixed-top  bg-light">
		<div class="container-fluid">
			<div class="collapse navbar-collapse" id="navbarNav">
				<ul class="nav navbar-nav">
					<li class="nav-item">
						<a class="nav-link" href="lesson"><span class="glyphicon glyphicon-remove"></span> </a>
					</li>
				</ul>
				<h4 class='text-center'>Practice</h4>
				{{!-- <ul class="nav navbar-nav navbar-right">
					<li class="nav-item">
						<a class="nav-link" href="achievements"><span class="glyphicon glyphicon-tower"></span> </a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="favorites"><span class="glyphicon glyphicon-heart"></span> </a>
					</li>
					<li class="nav-item active">
						<a class="nav-link" href="#"><span class="glyphicon glyphicon-user"></span> </a>
					</li>
				</ul> --}}
			</div>
		</div>
	</nav>

	<br>
	<br>
	<br>
	<div class="container text-center">
		<h5>Tap to reveal translation</h5>

		<div id="phraseCarousel" class="carousel slide" data-ride="carousel">
			<ol class="carousel-indicators">
				{{!-- <li data-target="#phraseCarousel" data-slide-to="0" class="active"></li> --}}
				{{#each phrases}}
					<li data-target="#phraseCarousel" data-slide-to="{{@index}}" id="indicator{{@index}}"></li>
				{{/each}}
			</ol>
			<div class="carousel-inner">
				{{!-- <div class="carousel-item item active">
					<a href="#"><button type="button" class="btn btn-primary btn-block phrase-button" id="0">
						{{phrases.[0].french}}</button></a>
				</div> --}}
				{{#each phrases}}
				<div class="carousel-item item" id="{{@index}}">
					<a href="#"><button type="button" class="btn btn-primary btn-block phrase-button" id="card{{@index}}">
						{{french}}</button></a>
				</div>
				{{/each}}
			</div>
			<a class="left carousel-control" href="#phraseCarousel" role="button" data-slide="prev">
				<span class="glyphicon glyphicon-chevron-left"></span>
				<span class="sr-only">Previous</span>
			</a>
			<a class="right carousel-control" href="#phraseCarousel" role="button" data-slide="next">
				<span class="glyphicon glyphicon-chevron-right"></span>
				<span class="sr-only">Next</span>
			</a>
		</div>	

{{!-- 
		{{#each phrases-single}}
		<div class="container">
			<a href="#"><button type="button" class="btn btn-primary btn-block phrase-button">{{french}}
			</button></a>
		</div>
		<br>
		{{/each}} --}}
		<br>
		<p>Take a Quiz</p>
		<h1><a href="quiz"><span class="glyphicon glyphicon-circle-arrow-right"></span></a></h1>
	</div>
</body>
</html>
